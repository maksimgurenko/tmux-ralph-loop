# 01: Add the greeting function

**What to build:** A small Python module exposing `greet(name)` that returns `Hello, <name>!`.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] Calling `greet("Ada")` returns `Hello, Ada!`.
- [ ] An automated test verifies the greeting.
