# 02: Add a greeting command

**What to build:** A command that accepts a name and prints a greeting using the existing function.

**Blocked by:** [01: Add the greeting function](01-greeting.md).

**Status:** ready-for-agent

- [ ] The command accepts `Ada` and prints `Hello, Ada!`.
- [ ] An automated test verifies the command's output and successful exit.
