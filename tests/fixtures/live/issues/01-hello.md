# 01: Write a greeting

**What to build:** Create `results/hello.txt` containing exactly `hello` followed
by one newline. Verify its contents before checking the criterion below.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] `results/hello.txt` contains exactly `hello` followed by one newline.
