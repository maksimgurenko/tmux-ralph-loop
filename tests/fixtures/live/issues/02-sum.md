# 02: Solve a tiny sum

**What to build:** Read the completed greeting task, calculate `1 + 2`, and write
the answer as a single decimal integer followed by one newline to `results/sum.txt`.
Keep the greeting file intact. Verify both files before checking the criteria below.

**Blocked by:** [01: Write a greeting](01-hello.md).

**Status:** ready-for-agent

- [ ] `results/sum.txt` contains the correct answer to `1 + 2` and one newline.
- [ ] `results/hello.txt` still contains exactly `hello` followed by one newline.
